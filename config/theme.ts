import {
  Breakpoints,
  darken,
  lighten,
  PaletteMode,
  ThemeOptions,
  PaletteOptions,
} from "@mui/material";

export const breakpoints: { values: Breakpoints["values"] } = {
  values: {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1536,
  },
};
