// Allow user to select 2 players from any era, compare there stats
// If current, player can compare current season stats OR historical stats OR matchup specific stats
// If either player is retired, compare career vs. career

import { Box, Typography } from "@mui/material";

const Compare = () => {
  return (
    <Box>
      <Typography variant="h1">Compare</Typography>
    </Box>
  );
};

export default Compare;
