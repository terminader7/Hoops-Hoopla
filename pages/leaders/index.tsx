// This page is meant for showing the top 5 leaders in each statistical category, clicking on cat name will take player to players page with filter preselected

import { Box, Typography } from "@mui/material";

const Leaders = () => {
  return (
    <Box>
      <Typography variant="h1">Leaders</Typography>
    </Box>
  );
};

export default Leaders;
